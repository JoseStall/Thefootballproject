<div class="form-group">
    <label for="chooseteam">Team</label>
    <select class="form-control" id="chooseteam">
        <option>Choisir une equipe</option>
        <% @games.each do |game| %>
        <option><%= game.home_team.name %></option>
        <% end %>
    </select>
</div>

<%= form_tag("/jeux/matchs", method: "get") do %>
    <%= select_tag 'team', options_for_select(Game.all.collect{ |u| [u.home_team.name, u.id] }) %>
    <%= submit_tag("Search") %>
<% end %>

<div id="showteam"></div>

<table class="table">
<thead>
    <tr>
        <th> N° </th>
        <th>date</th>
        <th>Drapeau</th>
        <th>Equipe</th>
        <th>Score</th>
        <th>Equipe</th>
        <th>Drapeau</th>

    </tr>
</thead>
<tbody>

    <% @games.each do |game| %>
    
    <tr>
        <td><%= game.id %></td>
        <td><%= game.date %></td>
        <td><img style='width: 35px; height: auto' src="<%= game.home_team.flag %>"></td>
        <td><%= game.home_team.name %></td>
        <td> 0 - 0 </td>
        <td><%= game.visiting_team.name %></td>
        <td><img style='width: 35px; height: auto' src="<%= game.visiting_team.flag %>"></td>



    </tr>
    <% end %>
  
</tbody>
</table>

<script>
    $('#showteam').text('Portugal');
    $("#chooseteam").click(function() {
        $('#showteam').text($('#chooseteam').val());
        console.log($('#chooseteam').val());
    });
</script>

